{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        {{ form_start(taskForm, {'action' : path('task_edit', { 'id' : task.id })}) }}
            {{ form_widget(taskForm) }}

            <button type="submit" class="btn btn-success pull-right">Modifier</button>
        {{ form_end(taskForm) }}
    </div>
{% endblock %}
